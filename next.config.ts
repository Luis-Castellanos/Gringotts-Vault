import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  // typedRoutes turned off until all nav destinations are real pages.
  // Re-enable once Dashboard / Accounts / Transactions / Cashflow / Net Worth / Reports
  // are wired up.
};

export default nextConfig;
